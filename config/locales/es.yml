es:
  activerecord:
    models:
      user: "Usuario"
      profile: "Perfil"
    attributes:
      user:
        required: "es requerido"
      profile:
        required: "es requerido"
    errors:
      messages:
        required: "es requerido"
  errors:
    messages:
      required: "es requerido"
